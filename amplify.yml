version: 1
backend:
  phases:
    build:
      commands:
        - echo "Backend build skipped"
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npx prisma generate
        - npx prisma migrate deploy
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
